# Public runtime configuration (safe to expose to the browser).
# If you add secrets later (JWT/OAuth client secrets, etc), do NOT prefix them with NEXT_PUBLIC_.

# WordPress REST base URL (should end with /wp-json)
NEXT_PUBLIC_WORDPRESS_REST_URL=http://localhost:8881/wp-json

# WordPress GraphQL endpoint (requires the WPGraphQL plugin)
NEXT_PUBLIC_WORDPRESS_GRAPHQL_URL=http://localhost:8881/graphql
